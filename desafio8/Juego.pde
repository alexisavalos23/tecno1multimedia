class Juego {

  // PROPIEDADES (variables)
  // EstadoGeneralDelJuego = menu, jugar, ganar, perder
  // Vincent personaje;
  // Arreglo de carnero(s)
  // Arreglo de bloque(s)


  // CONSTRUCTOR (setup del objeto)
  // EstadoGeneralDelJuego = "menu";
  // inicializar los objetos: personaje, carnero(s), bloque(s)


  // METODOS (funciones) 
  // actualizar()
  // carneros.actualizar();
  // bloques.actualizar();
  
  // dibujar()
  // personaje.dibuVins();
  // copos.dibujar();
  // cenizas.dibujar();
  
  // resetear()
  // carneros.reciclar();
  // bolques.reciclar();
  
}
